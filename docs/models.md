This topic contains classes that represent a structure returned by the Discord
API.

All models are immutable. To construct your own models to send to the API, see
the [Builders] topic.

To obtain instances of models by making HTTP requests to the API, see the
[Managers] topic.


[Builders]: ./builders-topic.html
[Managers]: ./managers-topic.html
